"""Paper-1 experiment drivers (continuous differentiable view selection).

Each module is a self-contained study; run from the repo root as a module, e.g.

    python -m experiments.studies.kinematics --phantom moderate --seeds 0,1,2

See ``experiments/studies/README.md`` for the experiment-to-table mapping.
"""
